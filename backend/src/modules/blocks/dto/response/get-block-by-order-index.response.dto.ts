import { blockSchema } from '../../../../domain/schemas/blocks/block.schema';

/**
 * Get Block Response DTO
 *
 * Returns a block using the common block schema (discriminated union).
 * Note: This is a placeholder class for NestJS decorators. 
 * Services return the Block type directly from common schemas.
 */
export class GetBlockResponseDto {}

// Export schema for validation
export { blockSchema };

